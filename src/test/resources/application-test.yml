spring:
  main:
    web-application-type: none
  datasource:
    url: jdbc:h2:mem:taskdb-test;MODE=LEGACY;DB_CLOSE_DELAY=-1
    driver-class-name: org.h2.Driver
    username: sa
    password:
  jpa:
    hibernate:
      ddl-auto: create-drop
    open-in-view: false
